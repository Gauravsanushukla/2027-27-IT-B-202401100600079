# TODO: Define provider and resources once cloud target is decided (AWS/GCP/Azure)
# Example structure:
# provider "aws" { region = "ap-south-1" }
# module "vpc" { source = "./modules/vpc" }
# module "eks" { source = "./modules/eks" }
